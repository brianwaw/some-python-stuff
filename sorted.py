#in order to sort the list and maintain it's original state, use the sorted() function
cars = ['bmw', 'audi', 'toyota', 'subaru']
print(sorted(cars))
#in this sorted function,there is still usage of the reverse=true
